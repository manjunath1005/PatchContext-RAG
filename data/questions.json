[
    "What is the purpose of APIRouter in FastAPI?",
    "How does FastAPI's dependency injection system work using Depends()?",
    "What is the role of BackgroundTasks in FastAPI?",
    "Why did FastAPI add support for custom OpenAPI operation IDs?",
    "Why does FastAPI allow overriding the default JSONResponse class?",
    "Why does FastAPI support form data parsing distinct from JSON body parsing?",
    "Why does FastAPI raise a clear error when both a request body and form data are declared together?",
    "Why does FastAPI support dependency injection for security scopes in OAuth2 flows?",
    "Why did FastAPI re-implement on_event internally for compatibility instead of removing it immediately when lifespan was introduced?",
    "Why does FastAPI cache the result of a dependency so it isn't called multiple times within the same request?",
    "Why did FastAPI refactor its internals to preserve APIRouter and APIRoute instances instead of recreating them on each include?",
    "Why does FastAPI use the same Annotated-based parameter style for Cookie, Header, Query, and Body declarations?",
    "Why does FastAPI treat a parameter typed as `int | str` as accepting either type instead of enforcing just one?",
    "Why did FastAPI need to support arrays (union types) for the \"type\" field when generating OpenAPI schemas?",
    "Why did FastAPI raise its minimum required Pydantic v2 version in a patch release?",
    "Why does FastAPI need to support both typing.TypeAliasType (Python 3.12+) and older type alias syntax?",
    "Why does FastAPI's TestClient work for testing FastAPI apps even though it was originally built for Starlette apps?",
    "Why did FastAPI wrap validation exception errors with jsonable_encoder when returning error responses?",
    "Why does FastAPI use a separate response_model parameter instead of inferring it from the function's return type annotation?",
    "Why does FastAPI use a cache_key to detect when two dependencies share the same sub-dependency instance?",
    "Why are FastAPI dependencies currently stored at the APIRoute level rather than at the APIRouter or FastAPI app level?",
    "Why do both FastAPI's middleware decorator and add_middleware() approaches risk multiple instantiations when used with mounted sub-apps?",
    "Why does FastAPI provide response_model_exclude_unset as a separate option instead of always excluding unset fields by default?",
    "Why do FastAPI maintainers recommend using the dependency_overrides provider instead of custom config-based mocking for tests?",
    "Why does FastAPI consider fine-grained control over query parameter data encoding a low priority compared to standard client-side adjustments?",
    "Why does FastAPI keep dependency resolution scoped per-request instead of allowing app-level global dependency registration?",
    "Why does FastAPI support multiple dependencies with the same sub-dependency without recomputation?",
    "Why did FastAPI move to Pydantic v2 despite breaking changes?",
    "Why does FastAPI use Annotated types for dependency and parameter declarations?",
    "Why does FastAPI include security utilities like OAuth2PasswordBearer out of the box?",
    "Why does FastAPI generate example values in the OpenAPI schema from Pydantic models?",
    "Why does FastAPI support WebSockets as a first-class feature?",
    "Why did FastAPI add support for generic Pydantic models in responses?",
    "Why do FastAPI maintainers review dependency version bumps for potential breaking changes before merging them?",
    "Why are release-preparation pull requests kept separate from feature pull requests in FastAPI's workflow?",
    "Why do FastAPI maintainers coordinate documentation updates across multiple pull requests during major changes?",
    "Why does FastAPI's CI workflow use a dedicated token for the bump-pre-commit-hooks process?",
    "Why does FastAPI require dependencies to be declared as callables in the function signature rather than resolved as global state?",
    "Why does mounting a sub-application under an APIRouter behave differently than mounting it directly on the FastAPI app?",
    "Why can't FastAPI's jsonable_encoder automatically handle every possible Python object type?",
    "Why doesn't FastAPI's websocket_route perform dependency injection the way regular HTTP routes do?",
    "Why can't the base Exception class be overridden using @app.exception_handler in FastAPI?",
    "Why does FastAPI apply response_model_by_alias only to the response_model rather than to the object returned by the function?",
    "Why did blocking pydantic.v1 model support cause migration problems for large codebases adopting FastAPI's Pydantic v2 support?",
    "Why did FastAPI need special handling to include Enum schema definitions in OpenAPI components?",
    "Why does FastAPI support generating separate OpenAPI schemas per route via separate_openapi_schemas?",
    "Why is setting CORS headers on server error responses treated as an unresolved design question in FastAPI?",
    "Why does adding GraphQL support to FastAPI require duplicating schema definitions separately from Pydantic models?",
    "Why does making a nested Pydantic model's fields Optional not automatically remove it from FastAPI's generated 'required' schema list?",
    "Why does FastAPI's OpenAPI schema generation differ between minor versions even when no explicit runtime code changes were documented?"
]